import { list } from './list';
import { catalogProducts } from './catalogProducts';

export const allProducts = [...list, ...catalogProducts];

// Файл allProducts.js служит точкой агрегации данных о товарах для всего приложения. 
// Он импортирует два набора товаров — list (основной список) и catalogProducts (дополнительный каталог) — и объединяет их в единый массив allProducts, который затем экспортируется.

// Это компромиссное решение, принятое для упрощения передачи данных:
// вместо работы с двумя источниками компоненты используют один общий список. 
// В разработке реального проекта потребовалась бы более надёжная обработка данных , например дедупликация и валидация.